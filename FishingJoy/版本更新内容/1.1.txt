
一、项目里将一些字符串判断，改成枚举类型判断
新建CommonEnum脚本，
将升级枪的切换类、货币类型、货币目标位置更新为枚举值，并统一放到CommonEnum脚本里管理；
同时更新货币相关预制体的类型绑定设置

二、项目中存在的拼写错误纠正
1、Treasour类名，类中diamands字段，以及UI_Treasour预制体，以及子物体Img_Treasure、But_Treasure；
改为对应的：Treasure，diamonds

2、Gun里bullect相关字段
改为：bullet

三、热更修复
（一）点击宝箱领取的金币钻石太拥挤，需要分散处理

1、C#边准备工作（前期开发就应该做的处理）
（1）Treasour脚本类引入Xlua库，Treasour类打上Hotfix标签
（2）CreatePrize打上LuaCallCSharp标签，
CreatePrize方法里有调用C#方法，lua那边重写也会用到C#方法；

3、Fish.lua脚本里重写Treasure类的CreatePrize方法

4、出现的报错：InvalidOperationException: try to dispose a LuaEnv with C# callback!

原因：通过xlua.hotfix(class, method, func)注入到C#，则通过xlua.hotfix(class, method, nil)删除；

解决方法：
（1）新建Dispose.lua脚本，执行反注册释放
（2）然后HotfixScript脚本的OnDisable里执行Dispose.lua脚本，置空
放在OnDestory里执行，会导致还未完全释放，就执行luaEnv的Dispose，还是会报错

（二）玩家金币钻石不够时没有相应的处理
1、C#边准备工作
Gun脚本，Attack方法，引入库，打标签

2、Fish.lua脚本里重写Attack方法

3、方便测试，把Gun脚本里金币和钻石的初始值改小







